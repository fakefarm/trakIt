%h1 Listing checkouts

%table.table
  %tr
    %th Date Out
    %th User
    %th Item
    %th Due Date
    %th Status
    

  - @checkouts.each do |checkout|
    %tr
      %td= link_to checkout.created_at.strftime("%b %e"), checkout
      %td= checkout.user.name
      %td= checkout.item.name
      %td
        = distance_of_time_in_words(Time.now, checkout.due_date) unless checkout.due_date.nil?
        \(
        = checkout.due_date.strftime("%B %e") unless checkout.due_date.nil?
        \)
      %td= checkout.status
      %td= link_to 'Edit', edit_checkout_path(checkout)
      %td= link_to 'Destroy', checkout, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Checkout', new_checkout_path, class: "btn btn-success"
