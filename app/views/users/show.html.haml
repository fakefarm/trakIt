%p#notice= notice

%h1= @user.name

= link_to 'Edit', edit_user_path(@user)
\|
= link_to 'Back', users_path

= render 'user_history_form'

<hr>
%h2= "#{pluralize(@user.assets.count, "asset")}  checked out"


%table.table
  %tr
    %th Name
    %th Date Out
    %th Status
    %th Checkout Serial
  - @checkout.each do |thing|
    %tr
      %td= link_to thing.asset.name, asset_path(thing.asset_id)
      %td= thing.asset.created_at.strftime("%B %e")
      %td= thing.status
      %td Query where item user_id == user_id

%h2 User History
%table.table
  %tr
    %th Comment
    %th 
  - @user.user_comments.each do |entry|
    %tr
      %td.span8= entry.comment
      
      %td.span4= entry.created_at.strftime("%B %e")


